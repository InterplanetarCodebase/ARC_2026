# Rover Development Log

Date: 2026-03-19  
Author: Khaja  
Branch: main  
Commit Hash: N/A  

---

## 1. Objectives for Today
What you planned to achieve today.

- Verify whether true end-to-end video latency measurement exists in current transmitter/receiver code.
- Replace ping-based metric with real source-to-render latency instrumentation.
- Improve startup debug visibility and confirm active IP usage paths for rover/base operation.

---

## 2. System Configuration
System state for testing

Hardware:
- Computer: Linux workstation
- Camera: `/dev/video0` (reference device)
- GPS / RTK: N/A
- Microcontrollers: N/A
- Other sensors: N/A

Software:
- OS: Linux
- ROS Version: N/A
- Workspace: `/home/khaja/Documents/ARC_2026`
- Important packages: GStreamer, GTK3 (PyGObject), Python standard sockets/JSON/time

Note:
- Validation done via diagnostics/static checks after refactor updates in transmitter and receiver scripts.

---

## 3. Work Performed
Implementations from today.

- Confirmed existing metric was not true video latency (it was ICMP ping + FPS display).
- Implemented true frame-linked latency scheme between transmitter and receiver:
  - Transmitter now emits per-frame telemetry payload (`camera_id`, `pts`, `sender_ts_ns`) over UDP side-channel.
  - Receiver now maps incoming frame `PTS` to telemetry timestamps and computes latency at render time.
  - HUD metric now shows end-to-end latency (ms) instead of ping.
- Added per-camera latency side-channel port strategy:
  - `latency_port = video_port + 1000` on both sides.
  - Runtime camera port changes also update associated latency port.
- Added startup diagnostics updates in both scripts to print latency telemetry defaults/notes.
- Updated receiver/transmitter header/startup text to reflect latency measurement usage.
- Verified no syntax/diagnostic errors after final patches:
  - `Camera/test_reciever.py` -> no errors
  - `Camera/test_transmitter.py` -> no errors
- Documented current rover/base IP path usage for data/control direction:
  - Rover/transmitter sends video + latency telemetry to base IP.
  - Base/receiver sends control commands back to rover IP.

---

## 4. Commands Executed
Important commands used during testing.

```bash
python3 test_transmitter.py -i <base_station_ip>
python3 test_reciever.py -i <rover_ip>
```
